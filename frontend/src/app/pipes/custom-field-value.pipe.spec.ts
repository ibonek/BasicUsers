import { CustomFieldValuePipe } from './custom-field-value.pipe';

describe('CustomFieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
